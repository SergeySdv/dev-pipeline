# Example env for local DevGodzilla + Windmill development.
#
# IMPORTANT:
# - `DEVGODZILLA_WINDMILL_TOKEN` is a server-side secret (never expose to browsers).
# - Values here are placeholders; do not commit real tokens.

# DevGodzilla API base URL (used by Windmill scripts/apps if needed)
DEVGODZILLA_API_URL=http://localhost:8080

# Windmill API (server-side; used by DevGodzilla to proxy calls)
DEVGODZILLA_WINDMILL_URL=http://localhost:8000
DEVGODZILLA_WINDMILL_WORKSPACE=devgodzilla
DEVGODZILLA_WINDMILL_TOKEN=wm_XXXXXXXXXXXXXXXXXXXXXXXX

# Optional: secure DevGodzilla API surface (enabled when set)
# DEVGODZILLA_API_TOKEN=change-me
# DEVGODZILLA_CORS_ORIGINS=http://localhost:8080,http://localhost:8000

# Optional: secure inbound webhooks (Windmill/CI must send `X-DevGodzilla-Webhook-Token`)
# DEVGODZILLA_WEBHOOK_TOKEN=change-me
