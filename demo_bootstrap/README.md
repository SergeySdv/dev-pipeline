# Demo Bootstrap

This tiny demo app exists so harnesses and onboarding flows have something to copy.

- `app.py` — minimal handler with a `hello()` function.
- `tests/test_app.py` — sanity check for the demo helper.
