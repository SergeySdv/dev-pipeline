apiVersion: apps/v1
kind: Deployment
metadata:
  name: devgodzilla-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: devgodzilla-worker
  template:
    metadata:
      labels:
        app: devgodzilla-worker
    spec:
      containers:
        - name: worker
          image: devgodzilla-core:latest
          imagePullPolicy: IfNotPresent
          command: ["python", "scripts/rq_worker.py"]
          env:
            - name: DEVGODZILLA_DB_URL
              valueFrom:
                secretKeyRef:
                  name: devgodzilla-secrets
                  key: db_url
            - name: DEVGODZILLA_REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: devgodzilla-secrets
                  key: redis_url
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            exec:
              command: ["bash", "-c", "test -f /tmp/worker-live || touch /tmp/worker-live"]
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            exec:
              command: ["bash", "-c", "test -f /tmp/worker-ready || touch /tmp/worker-ready"]
            initialDelaySeconds: 5
            periodSeconds: 10
