apiVersion: apps/v1
kind: Deployment
metadata:
  name: deksdenflow-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: deksdenflow-worker
  template:
    metadata:
      labels:
        app: deksdenflow-worker
    spec:
      containers:
        - name: worker
          image: deksdenflow-core:latest
          imagePullPolicy: IfNotPresent
          command: ["python", "scripts/worker.py"]
          env:
            - name: DEKSDENFLOW_DB_URL
              valueFrom:
                secretKeyRef:
                  name: deksdenflow-secrets
                  key: db_url
            - name: DEKSDENFLOW_REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: deksdenflow-secrets
                  key: redis_url
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            exec:
              command: ["bash", "-c", "test -f /tmp/worker-live || touch /tmp/worker-live"]
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            exec:
              command: ["bash", "-c", "test -f /tmp/worker-ready || touch /tmp/worker-ready"]
            initialDelaySeconds: 5
            periodSeconds: 10
