apiVersion: apps/v1
kind: Deployment
metadata:
  name: devgodzilla-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: devgodzilla-api
  template:
    metadata:
      labels:
        app: devgodzilla-api
    spec:
      containers:
        - name: api
          image: devgodzilla-core:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: DEVGODZILLA_DB_URL
              valueFrom:
                secretKeyRef:
                  name: devgodzilla-secrets
                  key: db_url
            - name: DEVGODZILLA_REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: devgodzilla-secrets
                  key: redis_url
            - name: DEVGODZILLA_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: devgodzilla-secrets
                  key: api_token
          ports:
            - containerPort: 8000
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: devgodzilla-api
spec:
  selector:
    app: devgodzilla-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
